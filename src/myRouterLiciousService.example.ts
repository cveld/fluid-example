// copy this file to myRouterLiciousService.ts and fill in your own details
import { RouterliciousService } from "@fluid-experimental/get-container";

export const service = new RouterliciousService({
    tenantId: "yourtenant",
    key: "yourkey",
    orderer: "yourorderer",
    storage: "yourstorage"
});